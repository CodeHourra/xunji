# 寻迹 v0.1.2 任务跟踪

> **关联**：在 v0.1.1 基础上增量发布；核心为 **CodeBuddy CLI 会话采集**。  
> **PRD**：[`docs/PRD/寻迹（XunJi）产品需求文档 PRD.md`](../PRD/寻迹（XunJi）产品需求文档 PRD.md)（CodeBuddy CLI JSONL）

---

## 任务

| ID | 任务 | 状态 | 说明 |
|----|------|------|------|
| **T01** | CodeBuddy CLI：`~/.codebuddy/projects/**.jsonl` 解析，接入 `scheduler` | 已完成 | `codebuddy_cli.rs`、`scheduler.rs` |
| **T02** | 默认配置 + 升级合并 `codebuddy-cli`（默认关闭采集） | 已完成 | `config.rs` |
| **T03** | 设置页：已接入采集器说明含 CodeBuddy CLI | 已完成 | `SettingsModal.vue` |
| **T04** | 版本号 0.1.2、CHANGELOG | 已完成 | 各 `package.json`、`Cargo.toml`、`tauri.conf.json` |

## 变更记录

| 日期 | 说明 |
|------|------|
| 2026-03-22 | 初版：CLI 采集与配置合并 |

---

## 与 v0.2.0 的关系

- **CodeBuddy CLI** 已从 v0.2.0 计划中的「数据采集」提前在本版本交付。  
- **CodeBuddy JetBrains**（多路径 / SQLite / XML）仍归后续版本（v0.2.x 或独立小版本）。
